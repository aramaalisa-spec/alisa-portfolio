---
const { class: className = '' } = Astro.props;
---

<article
	class={
		`prose prose-neutral dark:prose-invert prose-headings:[color:var(--color-ink)] ${className}`.trim()
	}
>
	<slot />
</article>